# justbc - A third-party Bandcamp player that runs in the terminal

## Configuration

A configuration file will be generated at `<your config directory>/reslario/justbc/config`. It will allow you to view and remap the keyboard controls, among other things. See [this](https://docs.rs/dirs/latest/dirs/fn.config_dir.html) for your platform's config directory.
