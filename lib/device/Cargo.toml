[package]
name = "device"
version = "0.1.0"
authors = ["reslario <reslario.code@gmail.com>"]
edition = "2018"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3.9", features = ["winerror", "guiddef", "wtypes", "minwindef", "combaseapi", "winnt", "mmdeviceapi"] }
